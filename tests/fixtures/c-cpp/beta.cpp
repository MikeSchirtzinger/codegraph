#include "beta.h"

#include <iostream>

namespace beta {

// The other of the two unrelated `helper` definitions. See alpha.cpp and
// gamma.cpp (case c, D3).
void helper() {
    std::cout << "beta helper" << std::endl;
}

}  // namespace beta
