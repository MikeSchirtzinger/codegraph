#include "alpha.h"

#include <iostream>

namespace alpha {

// One of two unrelated namespaces that define a function named `helper`.
// See beta.cpp and gamma.cpp (case c, D3).
void helper() {
    std::cout << "alpha helper" << std::endl;
}

}  // namespace alpha
