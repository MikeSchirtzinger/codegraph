/**
 * Case b (D2) resolution target. Qualified name `src::db::connection::connect`
 * (TypeScript keeps the full relative file path, unlike Rust's crate-root
 * elision — see ../../README.md "Contract interpretations").
 */
export function connect(): void {
  // Pretend to open a connection.
}
