"""Case b (D2) resolution target. Qualified name `db::connection::connect`
(file path `db/connection.py`; Python keeps the full relative path, like
TypeScript — see ../../README.md "Contract interpretations")."""


def connect():
    """Pretend to open a connection."""
