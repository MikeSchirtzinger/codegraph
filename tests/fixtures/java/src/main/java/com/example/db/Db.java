package com.example.db;

/**
 * Case b (D2) resolution target. Qualified name `com::example::db::Db::connect`
 * (directory mirrors the package declaration, standard Java convention —
 * the Maven layout prefix `src/main/java/` is elided; see
 * ../../../../../../README.md "Contract interpretations").
 */
public class Db {
    public static void connect() {
        // Pretend to open a connection.
    }
}
