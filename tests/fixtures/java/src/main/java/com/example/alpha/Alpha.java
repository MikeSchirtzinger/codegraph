package com.example.alpha;

/**
 * One of two unrelated classes that define a static method named `helper`.
 * See Beta.java and Gamma.java (case c, D3).
 */
public class Alpha {
    public static void helper() {
        System.out.println("alpha helper");
    }
}
