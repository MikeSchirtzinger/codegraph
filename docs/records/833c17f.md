# Verbatim development-history record

Quoted from the private development history with `git show -s 833c17f`.
The measurements below predate this repository's public history and are
reproduced here as the record the posts and README cite.

```
commit 833c17f357ce8ee2ef8cf11231d0a719d8546246
Author: Mike Schirtzinger <155995654+MikeSchirtzinger@users.noreply.github.com>
Date:   Wed Jul 22 01:36:16 2026 -0400

    fix(gate): stale scan mirrors resolve_one — clean tree, clean verdict
    
    `find_stale_references` bare-tailed every UNRESOLVED edge onto the queried
    name, the one fallback `resolve_one` explicitly forbids. A fresh self-index
    therefore rejected an UNMODIFIED src/graph/dependencies.rs with 38 stale
    references — 37 of them `cursor.node()`, the tree-sitter method call the
    extractor itself makes, colliding with that module's `fn node()` test
    helper; the 38th a crate-prefixed call to a symbol that is right there and
    unrenamed. A gate that rejects a tree nobody touched trains its operator to
    ignore it.
    
    Three changes:
    
    1. The scan now applies the cascade's own rule. A bare capture matches
       within resolve_one's candidate-pool key (bare name + to_type + language
       family — `Lcg { .. }` captures as a *function* and was never routed to
       the live struct). A qualified capture must be exact-or-`::`-suffix
       compatible with a live definition's qualified_name, in that direction
       only. Only when the queried name has no live definition left — the
       renamed-away case the scan exists for, reached through deletion
       tracking — does the bare tail alone decide, which is the kill-test's
       shape and stays matched. `resolve::language_family` is now pub for the
       same reason `normalize_separators`/`bare_name` already were: query-time
       matching must use the resolver's table, not a second copy of it.
    
    2. Extraction drops any capture containing a newline, at add_name_edge —
       the one choke point all six extractors share. A name is a single-line
       token; a multi-line to_name is a wrapped receiver chain captured whole
       (`items\n.iter()\n.find`), unresolvable by construction, whose bare tail
       collides freely with real symbol names. Measured on the frontier tree:
       759 dropped, edges 5,384 -> 4,625, UNRESOLVED 4,219 -> 3,460, with
       RESOLVED (946), AMBIGUOUS (28), file_refs (106) and fingerprints (698)
       bit-identical — an UNRESOLVED edge was never a fingerprint input.
    
    3. tests/gate_specificity.rs: the missing specificity half. A tree that is
       correct by construction, carrying each false-positive capture shape,
       asserted clean for every file one at a time and for all files at once;
       plus the converse on the same tree (one incomplete rename must still
       reject) so neither property can be bought by weakening the other. All
       three fail against the pre-fix code with exactly the shapes above.
    
    Measured, fresh self-index, every source file queried on its own:
    1,015 stale references across 37 files -> 0 across every file. The
    acceptance repro (gate_verdict on unmodified src/graph/dependencies.rs)
    goes 38 -> 0, verdict clean. Suite 229 -> 240 green (4 unit tests, counted
    twice since main.rs re-declares the module tree, + 3 integration).
    
    Not fixed, out of this fix's code path and file scope: the CLI/MCP line
    "no live symbol currently has this name" (src/main.rs:399,
    src/mcp/server.rs:198) prints unconditionally whenever stale_references is
    non-empty, including when the query DID match live symbols. It is a
    message-construction bug in the printer, not in the scan.
```
