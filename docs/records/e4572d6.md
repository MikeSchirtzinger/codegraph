# Verbatim development-history record

Quoted from the private development history with `git show -s e4572d6`.

<!-- credo-lint:allow-fenced verbatim commit message quoted from the private development history -->
```
commit e4572d6bcce15e1017f28c380ca83ad2e39ad404
Author: Mike Schirtzinger <155995654+MikeSchirtzinger@users.noreply.github.com>
Date:   Tue Jul 14 17:19:22 2026 -0400

    feat(gate): deletion tracking closes the incomplete-rename blind spot (§10 fix 1)
    
    Incremental re-index now records definitions that disappear from a file
    (rename/delete/move-out/vanished file) in a new deleted_symbol table:
    diffed by qualified_name via load_file_symbols (imports excluded), healed
    when the same (qualified_name, file_path) is re-defined — the pair, not
    the name alone: crate-root files legally collide — retained otherwise
    (rows are query targets, never verdicts, so retention is FP-free), wiped
    on --force (the spec'd degradation). R4 inputs are byte-identical.
    
    impact_verdict_for_paths unions deleted-from-touched-files names into its
    per-name loop, so find_stale_references — which always ran for by-name
    queries — finally fires for names the touched files no longer define.
    That was killtest-6's FAIL: the paths entry only queried live symbols, an
    incomplete rename removes the old name from that set by definition, and
    the gate passed silently. No StructuralVerdict contract change; brevity
    gets the catch through the existing stale_references rejection reason.
    
    Proof: tests/deletion_tracking.rs (7 tests — capture lifecycle, heal
    scoping under qn collision, import exclusion, force wipe, and the
    killtest scenario end-to-end through open_store on a real surrealkv
    store); examples/gate_verdict.rs for shell repro (self-index demo caught
    normalize_separators' 3 forgotten callers from the renamed file's path).
    Full suite 192 green, clippy clean.
```
